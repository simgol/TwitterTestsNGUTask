repository: git@github.com:simgol/TwitterTestsNGUTask.git

driver/browser: FireFox

tests:
1. "Verification if a logged user can add a new tweet"
2. "Verification if a logged user is able to follow and unfollow a profile in the application"

run tests by runnining following command in the command line: TODO: create testng.xml file and insert here the correct path/command